# Python dependencies for ebpf-testbed userspace tools
#
# This project uses libbpf CO-RE — no BCC required.
# System dependencies (install with apt):
#
#   sudo apt install libbpf-dev linux-tools-$(uname -r) clang llvm
#
# After installing system deps, generate vmlinux.h:
#   BPFTOOL=$(ls /usr/lib/linux-tools/*/bpftool | head -1)
#   sudo $BPFTOOL btf dump file /sys/kernel/btf/vmlinux format c > src/common/vmlinux.h
#
# See docs/setup.md for full installation instructions.

# Test runner
pytest>=7.0

# Optional: linting / formatting for Python files
pycodestyle>=2.10
